name: streakoo
description: A modern habit tracker with streaks and an AI coach.
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  lottie: ^3.1.2
  shared_preferences: ^2.3.2
  supabase_flutter: ^2.5.8
  google_sign_in: ^6.1.5
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4
  url_launcher: ^6.3.0
  flutter_heatmap_calendar: ^1.0.5
  http: ^1.2.2
  uuid: ^4.4.0
  # 🎉 Premium features
  confetti: ^0.7.0
  fl_chart: ^0.69.0
  vibration: ^2.0.0
  intl: ^0.19.0
  health: ^13.0.0  # Updated for Flutter compatibility

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/